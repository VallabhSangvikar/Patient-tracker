<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Patient Records</title>
  <link rel="stylesheet" href="/css/dashboard.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
  <div class="sidebar">
    <div class="sidebar-header">
      <h3>PATIENT RECORDS</h3>
    </div>
    <ul class="sidebar-menu">
      <li><a href="/dashboard" class="active"><i class="fas fa-tachometer-alt"></i>Dashboard</a></li>
      <li><a href="/addrecord"><i class="fas fa-plus"></i>Add Records</a></li>
      <li><a href="/doctors"><i class="fas fa-user-plus"></i>Add Doctor</a></li>
      <li><a href="/doctorslist"><i class="fas fa-user-plus"></i>Doctors list</a></li>
      <li><a href="/login"><i class="fas fa-history"></i>User Logs</a></li>
      <li><a href="/about"><i class="fas fa-history"></i>About Us</a></li>
    </ul>
  </div>
  <div class="content">
    <div class="table-header">
      <div class="show-entries">

        <span><b>Entries</b></span>
      </div>
      <%if(edit && edit.length!=0){ %>
      <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <strong><%= edit %></strong> You should check in on some of those fields below.
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
      <%}%>
      <div class="search-box">
        <input type="text" placeholder="Search...">
      </div>
    </div>
    <div class="table-container">
      <table class="table table-striped table-bordered">
        <thead>
          <tr>
            <th>Case No.</th>
            <th>Doctor's name</th>
            <th>Gender</th>
            <th>Age</th>
            <th>Contact No.</th>
            <th>Date Added</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          <% for( let i = 0; i < details.length; i++ ) { %>
            <tr>
              <td><%= details[i].patient_caseNo %></td>
             <td><%= details[i].doctor_name %></td>
             <td><%= details[i].doctor_gender %></td>
             <td><%= details[i].doctor_age %></td>
             <td><%= details[i].doctor_phone %></td>
             <td><%= details[i].date %></td>
             <td>
               <a class="btn btn-edit btn-warning" href="/edit/<%= details[i]._id %>">Edit</a>
               <a class="btn btn-view btn-primary" href="/details/<%= details[i]._id %>">View</a>
             </td>
           </tr>
          <% } %>
        </tbody>
      </table>
    </div>
    
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>